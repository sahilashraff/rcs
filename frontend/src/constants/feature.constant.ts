// Feature keys — must match the `key` field of each entry in
// backend/config/features.php exactly. This is the frontend's single
// source of truth for these strings: nav/route config imports them
// instead of repeating the literal, so a rename only needs updating here.
export const FEATURE_DASHBOARD = 'dashboard'
export const FEATURE_PERMISSIONS = 'permissions'
